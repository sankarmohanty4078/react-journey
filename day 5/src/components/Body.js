import { ResContainer } from "./ResContainer";
import Search from "./Search";

const Body = () => {
  return (
    <div className="body">
      <Search />
      <ResContainer />;
    </div>
  );
};

export default Body;
